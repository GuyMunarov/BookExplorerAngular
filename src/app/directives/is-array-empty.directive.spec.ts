import { IsArrayEmptyDirective } from './is-array-empty.directive';

describe('IsArrayEmptyDirective', () => {
  it('should create an instance', () => {
    const directive = new IsArrayEmptyDirective();
    expect(directive).toBeTruthy();
  });
});
